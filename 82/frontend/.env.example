# Local development backend (recommended)
VITE_API_BASE_URL=http://localhost:8081

# Optional: if you're running frontend locally but want to call remote backend,
# set this to true.
# VITE_USE_REMOTE_API=true
